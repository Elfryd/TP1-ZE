{% extends "base.html.twig" %}
{% block body %}
    <table>
        <tr>
            <th>Prénom</th>
            <th>Nom</th>
            <th>Discord</th>
            <th>Github</th>
            <th>email</th>
        </tr>
        {% for user in tabUser %}
            <tr>
                <td>{{ user.firstname }}</td>
                <td>{{ user.surname }}</td>
                <td>{{ user.discord }}</td>
                <td>{{ user.github }}</td>
                <td>{{ user.email }}</td>
            </tr>
        {% endfor %}
    </table>
{% endblock %}